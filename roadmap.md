Track 설계 (최종 확정본 — 2026.07.29 수정, 인프라 엔지니어→데브옵스 확장 반영)

## 커리어 목표
1차: 인프라 엔지니어 정규직 취업 (2027.06.21까지)
2차 확장: 데브옵스 엔지니어

## 학습 방식 공통 원칙: Top-Down 오리엔테이션
14개 트랙 전체에 "뼈대는 Top-Down, 깊이는 Bottom-Up 그대로" 원칙을 적용한다.
- 각 트랙 진입 시, 세부 항목 학습 전에 "0번 항목"으로 전체 구조/흐름을 그림 한 장으로 먼저 훑는다.
- 0번 항목은 30분~1시간 내외, 딱 한 번만 진행하며, 새 개념을 깊게 이해시키려 하지 않는다. "이런 컴포넌트가 이렇게 연결되어 있다"는 지도만 그린다.
- 1번 항목부터는 기존 방식(이론 → 실무 시나리오 → 명령어/설정 도출) 그대로 진행하며, 깊이 기준(학습 진행 원칙 문서 A/B/C 그룹 기준)은 절대 낮추지 않는다.
- 세부 항목이 끝날 때마다 "이게 0번 지도에서 어느 부분이었지?"를 짧게 되짚는다.

---

Track 1

[네트워크 파트] 스코프 정리
목표 (Goal)
암기식 이론이 아니라, 실제 장애 상황을 진단하고 원인을 계층별로 좁혀나갈 수 있는 감각을 기른다.
→ "OSI 7계층이 뭐다" 수준이 아니라 "지금 이 증상이 어느 계층 문제인지 파악하고, 어떤 명령어로 확인/조치할지 즉시 판단"이 목표.

우선순위
전체 14개 트랙 중 1순위 (진행 순서 1번, 평일 짬짬이 진행, 매일 진행)

학습 순서 및 핵심 키워드
0. [Top-Down 오리엔테이션]
- 사내 네트워크 전체 구조를 큰 그림으로 먼저 본다: 내 PC → 스위치/AP → 사내망 → 게이트웨이 → 방화벽 → 외부 회선(ISP)까지 흐름 파악
1. 기초 개념 재정립 (OSI 7계층, TCP/IP 4계층, MAC/IP/포트 개념)
- 이론 자체보다 "장애 시 이 계층에서 뭘 확인하나"와 매칭해서 암기
2. IP / 서브넷 / 라우팅 기초
- 서브넷 마스크 계산, 사설/공인 IP, NAT, 게이트웨이 이해 → 사내 IP 할당/충돌 트러블슈팅에 직결
3. 네트워크 장비 이해 (스위치/L2, 라우터/L3, AP)
- VLAN 개념, 포트 상태 확인, 물리 연결(케이블/포트) 점검 절차
4. 무선 네트워크(Wi-Fi) 기초
- 2.4GHz/5GHz 차이, 채널/간섭 개념, WPA2/3 인증 방식 → "특정 자리에서 와이파이 안 잡힘" 같은 실전 시나리오 대응
5. DNS
- nslookup/dig, DNS 캐시, hosts 파일, 사내 DNS 서버 이슈 진단
6. DHCP
- 임대 갱신, 충돌, 사내 DHCP 서버 로그 확인
7. 방화벽 / NAT / 포트포워딩
- 특정 포트 차단 여부 확인, 방화벽 정책으로 인한 접속 불가 진단
8. 트러블슈팅 도구/명령어 실전 (통합 복습)
- ping, tracert/traceroute, ipconfig/ifconfig, netstat, nslookup, telnet/nc, arp -a, pathping
- 4~7번에서 이미 등장한 도구 + 신규 도구를 한 번에 묶어 실전 숙달
9. 프로토콜 심화 (HTTP/HTTPS, TLS 기본 동작, TCP 3-way handshake, UDP 차이)
- 웹 서비스 장애 시 계층 구분에 활용
10. VPN 기초
- 사내 원격 접속 환경(VPN 터널) 구조 이해, 흔한 접속 실패 원인
11. 패킷 캡처 기초
- Wireshark 기본 사용법, 특정 통신 실패 시 패킷 레벨 확인

실무 적용 훈련 방식
매일 1개의 [현업 실무 시나리오 문제] 출제
이론 설명 → 바로 문제로 연결 → CLI 명령어 조합 도출까지 실습형으로 진행
사고 흐름: PC 자체 → 스위치/AP → 사내망 → 외부 회선(ISP) 순으로 좁혀나가는 흐름을 매번 반복 훈련

---

Track 2

[컴퓨터 구조 파트] 스코프 정리
목표 (Goal)
학부 수준의 이론 암기가 아니라, 서버/PC 하드웨어 이상 징후를 진단하고, OS/성능 트러블슈팅의 근본 원리를 이해할 수 있는 실전 감각을 기른다.

우선순위
전체 14개 트랙 중 1순위급 (진행 순서 2번, 평일 짬짬이 진행)

학습 순서 및 핵심 키워드
0. [Top-Down 오리엔테이션]
- 컴퓨터 하드웨어 전체 구성(CPU-메모리-스토리지-메인보드-전원부)이 어떻게 상호작용하는지, "느리다"는 증상이 어디서 발생할 수 있는지 큰 그림으로 먼저 본다
1. 컴퓨터 구성 기본 (하드웨어 구조 전반)
- CPU/메모리/스토리지/메인보드/전원부 역할과 상호작용, 부품 간 병목(bottleneck) 개념
2. CPU 구조 기초
- 코어/스레드, 클럭 속도, 캐시(L1/L2/L3) 개념, 멀티코어 vs 멀티스레딩 차이
3. 메모리(RAM) 구조
- 물리 메모리 vs 가상 메모리, 페이징/스와핑 개념
4. 스토리지 구조
- HDD vs SSD 동작 원리 차이, RAID 개념(0/1/5/10), IOPS/처리량 개념
5. 버스 & I/O 구조 (선택 항목 — 여유 있을 때)
- 시스템 버스, PCIe, 네트워크 카드/스토리지 컨트롤러 연결 구조
6. 프로세스/스레드 실행 원리 (OS와 하드웨어 경계)
- 컨텍스트 스위칭, 인터럽트 처리, 스케줄링 기본 개념
7. 부팅 과정의 하드웨어적 이해
- POST, BIOS/UEFI, 부팅 실패 시 하드웨어 레벨 진단(비프음, POST 코드)
8. 네트워크 하드웨어 기초
- NIC 동작 원리, MAC 주소 하드웨어적 이해
9. 서버 하드웨어 특화 지식
- 서버용 부품 차이(ECC 메모리, 서버용 CPU/RAID 카드), 핫스왑, 이중화(전원/디스크) 개념
10. 성능 진단 실전 연결
- "느리다"는 증상을 CPU/메모리/디스크/네트워크 중 어디 문제인지 구조적으로 좁혀나가는 진단 흐름
11. 하드웨어 진단 실전 도구 종합
- Windows: msinfo32, 장치 관리자
- macOS: system_profiler SPHardwareDataType, Activity Monitor
- 디스크 상태: smartctl (Windows/macOS 공통, smartmontools 설치)
- 메모리 진단: memtest86 개념 (실습은 선택)
- 서버 원격 관리(IPMI/iDRAC/iLO) (개념 이해 위주)

실무 적용 훈련 방식
매일 1개의 [현업 실무 시나리오 문제] 출제
이론 설명 → 실전 문제 → 진단 절차/명령어 조합 도출까지 실습형으로 진행

---

Track 3

[리눅스 운영체제 파트] 스코프 정리
목표 (Goal)
이론 암기가 아니라, 서버를 설계·구축하고, 운영 중 상태를 진단하고 문제를 해결할 수 있는 실전 감각을 기른다.

우선순위
전체 14개 트랙 중 1순위 (진행 순서 3번, 평일 짬짬이 진행, 매일 진행)

학습 순서 및 핵심 키워드
0. [Top-Down 오리엔테이션]
- 리눅스 서버 하나가 부팅부터 서비스 제공까지 어떤 레이어(부팅 → 커널 → 파일시스템 → 프로세스/서비스 → 네트워크)로 동작하는지 큰 그림으로 먼저 본다
1. 서버 초기 구축 & 보안 하드닝
- OS 설치 후 초기 설정, 불필요 서비스/포트 제거, 기본 보안 정책 수립, SELinux/AppArmor 기초
2. 기본 파일시스템 구조 & 디렉토리
- /etc, /var, /var/log, /proc, /home 등 위치별 역할
- (병행) vi/vim, nano 등 텍스트 에디터는 여기서부터 자연스럽게 손에 익히기
3. 파일/디렉토리 권한 관리
- chmod, chown, umask, sticky bit, 특수 권한
4. 프로세스 관리
- ps, top/htop, kill/killall, nice/renice, systemctl(프로세스 제어), 좀비 프로세스 처리
5. 패키지 관리
- yum/dnf (RHEL 계열), apt (Debian 계열), 의존성 문제 해결
6. 디스크/스토리지 설계 & 관리
- 파티션 설계 기준, LVM 설계 사고방식, df, du, fdisk/lsblk, mount/umount
7. 네트워크 설정 & 진단 (리눅스 관점)
- ip a, nmcli, /etc/hosts, /etc/resolv.conf, ss/netstat, iptables/firewalld 기초
8. 로그 분석
- /var/log/messages, syslog, journalctl, 로그 필터링(grep/awk/sed 조합)
9. 서비스/데몬 관리
- systemctl(서비스/부팅 관리), 서비스 자동시작 설정
10. 사용자/그룹 관리
- useradd/usermod, /etc/passwd, /etc/shadow, sudo 권한 설정, 계정 잠금/해제
11. 크론(Cron) & 스케줄링
- crontab, at, 배치 작업 실패 시 로그 확인
12. 쉘 스크립트 기초 문법 (선행 학습)
- 변수, 조건문, 반복문, 함수 → Python/Shell 자동화 트랙에서 심화
13. 성능 진단 도구
- vmstat, iostat, sar, free -h
14. 부팅 프로세스 & 트러블슈팅
- GRUB, runlevel/systemd target, 부팅 실패 시 복구 절차
15. SSH & 원격 접속 관리
- ssh-keygen, known_hosts, sshd_config 보안 설정, 접속 안 되는 원인 진단

실무 적용 훈련 방식
매일 1개의 [현업 실무 시나리오 문제] 출제
이론 설명 → 실전 문제 → 명령어 조합 도출까지 실습형으로 진행

---

Track 4

[윈도우 운영체제 파트] 스코프 정리
목표 (Goal)
암기식 GUI 조작법이 아니라, 사내 윈도우 서버/클라이언트 환경을 설계·구축하고, CLI/PowerShell로 실질적인 진단·조치를 할 수 있는 실전 감각을 기른다.

우선순위
전체 14개 트랙 중 1순위 (진행 순서 4번, 평일 짬짬이 진행, 매일 진행)

학습 순서 및 핵심 키워드
0. [Top-Down 오리엔테이션]
- 사내 윈도우 인프라 전체 구조(도메인 컨트롤러 - AD/DNS/DHCP - GPO - 클라이언트 PC)가 어떻게 맞물려 돌아가는지 큰 그림으로 먼저 본다
1. 윈도우 서버 초기 구축 & 역할(Role) 설계
- Server Manager, 역할/기능 추가(AD DS, DNS, DHCP, 파일서버 등), 신규 서버 설치 시 초기 보안 베이스라인
2. Active Directory (AD) 설계 & 기초
- 도메인/포리스트/OU 구조 설계, 신규 도메인 구축 시 고려사항, 사용자·그룹·컴퓨터 객체 관리, dsquery/dsget
3. 그룹 정책(GPO) 설계 & 운영
- GPO 설계 원칙, gpupdate/gpresult, GPO 적용 순서(LSDOU), 정책 미적용 문제 진단
4. DNS/DHCP (윈도우 서버 관점)
- 윈도우 DNS 서버 관리, DHCP 임대/예약 설정, 사내 IP 충돌·조회 실패 트러블슈팅
5. 파일 시스템 & 권한 관리
- NTFS 권한 vs 공유 권한, icacls, 공유 폴더 설정
6. 프로세스 & 서비스 관리
- 작업 관리자, tasklist/taskkill, services.msc, sc 명령어
7. 이벤트 로그 분석
- 이벤트 뷰어, wevtutil, 로그온 실패/시스템 오류 이벤트 ID 기반 원인 추적
8. PowerShell 기초 & 실전 스크립팅
- Get-Process, Get-Service, Get-EventLog, Get-ADUser 등 cmdlet 조합
9. 원격 관리 & 원격 접속
- RDP, WinRM/PSRemoting, 원격 지원 툴 활용
10. 백업/복구 & 시스템 이미징
- Windows Backup, 시스템 복원, PC 이미징(WDS/MDT 개념)
11. 업데이트 관리 (WSUS/Windows Update)
- 패치 배포 정책, 업데이트 실패 진단
12. 보안 설정 & 로컬 정책
- 로컬 보안 정책(secpol.msc), 방화벽
13. 부팅 문제 & 시스템 복구
- 안전 모드, bcdedit, 부팅 실패 시 복구 환경(WinRE) 활용
14. 클라이언트 PC 관리 (엔드포인트)
- EPP/백신 관리, 원격 소프트웨어 배포

실무 적용 훈련 방식
매일 1개의 [현업 실무 시나리오 문제] 출제
이론 설명 → 실전 문제 → CLI/PowerShell 명령어 조합 도출까지 실습형으로 진행

---

Track 5

[Python / Shell Script 자동화 파트] 스코프 정리
목표 (Goal)
문법 암기가 아니라, 반복적인 수작업 업무를 스크립트로 자동화하고, 그 자동화를 안전하게 운영에 배포할 수 있는 실전 역량을 기른다.

우선순위
전체 14개 트랙 중 1순위급 (진행 순서 5번, 주말 진행)

학습 순서 및 핵심 키워드
0. [Top-Down 오리엔테이션]
- "수작업 반복 업무 → 스크립트화 → 스케줄링/배포 → 실패 시 알림"까지 자동화 전체 흐름을 큰 그림으로 먼저 본다
1. Shell Script 기초 문법
- 변수, 조건문(if/case), 반복문(for/while), 함수, 인자 처리
2. Shell Script 실전 패턴
- 텍스트 처리 조합(grep/awk/sed/cut), 파이프라인 활용
3. Shell Script 실무 자동화 예제
- 디스크 사용량 임계치 알림, 로그 패턴 알림, 오래된 로그 자동 삭제
4. Python 기초 문법
- 변수/자료형, 조건문/반복문, 함수, 컬렉션 컴프리헨션, 예외 처리
5. Python 파일/텍스트 처리
- 파일 읽기/쓰기, 정규표현식(re), CSV/JSON 파싱
6. Python 시스템 연동
- os/subprocess 모듈로 쉘 명령 실행
7. Python 네트워크/API 자동화
- requests 라이브러리로 API 호출
8. Python DB 연동
- pymysql/psycopg2 등으로 DB 접근, 자동 리포트 생성
9. Python 서버 자동화 (paramiko 등)
- SSH 원격 명령 실행, 여러 서버 동시 배포/점검
10. AWS 자동화 (boto3)
- EC2 상태 체크, S3 업로드/다운로드 자동화
11. 예외 처리 & 로깅
- logging 모듈, 실패 시 알림 처리, 재시도(retry) 로직
12. 안전한 자동화 배포 (테스트/롤백)
- dry-run 모드 구현, 실행 전 시뮬레이션 검증, 실패 시 롤백 절차
13. 스케줄링 & 배포
- crontab/작업 스케줄러 등록, venv 관리, requirements.txt
14. Windows 자동화 (PowerShell 연계)
- Python-PowerShell 역할 분담, pywin32

실무 적용 훈련 방식
매일 1개의 [현업 실무 시나리오 문제] 출제
이론 설명 → 실전 문제 → 스크립트 작성까지 실습형으로 진행

---

Track 6

[보안 기초 파트] 스코프 정리
목표 (Goal)
자격증 이론 암기가 아니라, 사내 보안 위협을 실제로 탐지·차단·대응할 수 있는 실전 감각을 기른다.

우선순위
전체 14개 트랙 중 1순위급 (진행 순서 6번, 평일 짬짬이 진행)

학습 순서 및 핵심 키워드
0. [Top-Down 오리엔테이션]
- 사고 대응 흐름(탐지→격리→분석→복구→보고) 전체를 먼저 그림으로 훑고, 각 보안 요소(방화벽/EPP/접근제어/암호화)가 이 흐름 어디에 위치하는지 파악
1. 방화벽 정책 설계 & 관리
- 인바운드/아웃바운드 규칙 원칙, 화이트리스트 vs 블랙리스트
2. 백신/EPP(Endpoint Protection) 관리
- EPP 신호 미탐지 시 원격 실사 절차, 강제 설치/재설치 프로세스
3. 접근제어 (계정/권한) 원칙
- 최소 권한 원칙, 계정 잠금 정책, MFA(다단계 인증)/SSO 개념
4. 취약점 점검 기초
- 취약점 스캐너 개념(Nessus/OpenVAS), CVE 개념 및 조회 방법
5. 로그 기반 위협 탐지
- 비정상 로그인 시도 탐지, 비정상 트래픽 패턴 인지
6. 랜섬웨어 대응 절차
- 감염 징후 초기 인지, 네트워크 격리 절차, 백업 기반 복구 프로세스
7. 사내 네트워크 보안 구조
- VLAN을 통한 네트워크 분리, DMZ 개념, VPN 보안 설정
8. 웹 보안 기초 (자체 개발 웹앱 대상)
- SQL Injection/XSS 기본 개념과 방어법
9. 암호화 기초
- 대칭키/비대칭키 개념, HTTPS/TLS 동작 원리, 비밀번호 해싱(bcrypt)
10. 보안 정책 & 사고 대응 프로세스
- 사고 대응 흐름(탐지→격리→분석→복구→보고)

실무 적용 훈련 방식
매일 1개의 [현업 실무 시나리오 문제] 출제
이론 설명 → 실전 문제 → CLI/정책 설정 조합 도출까지 실습형으로 진행

---

Track 7

[AWS 클라우드 파트] 스코프 정리
목표 (Goal)
자격증 취득용 암기가 아니라, 실제 사내 인프라를 클라우드로 전환·운영할 때 마주치는 문제를 진단·조치할 수 있는 실전 감각을 기른다.

우선순위
전체 14개 트랙 중 2순위 (진행 순서 7번, 주말 진행)

학습 순서 및 핵심 키워드
0. [Top-Down 오리엔테이션]
- 사내 인프라 하나가 AWS로 전환된다면 어떤 그림이 되는지(VPC 안에 EC2/RDS가 있고, IAM이 권한을 통제하고, CloudWatch가 지켜보는 구조) 먼저 큰 그림으로 본다
1. 클라우드 기본 개념 & AWS 계정/IAM
- On-premise vs Cloud, Region/AZ 개념, IAM 사용자/그룹/정책, MFA 설정
2. VPC (가상 사설 네트워크)
- VPC/서브넷 설계, 라우팅 테이블, 인터넷/NAT 게이트웨이
3. EC2 (컴퓨팅)
- 인스턴스 생성/타입 선택, 보안 그룹 vs 네트워크 ACL, SSH 접속 문제 진단
4. EBS & 스토리지 기초
- 볼륨 생성/연결/스냅샷, S3 기본 개념
5. 보안 그룹 & 네트워크 ACL 심화
- 인바운드/아웃바운드 규칙 설계, 포트 접속 불가 트러블슈팅
6. RDS (관리형 DB)
- DB 인스턴스 생성, 백업/스냅샷, 연결 실패 진단
7. IAM 심화 & 최소 권한 원칙
- 정책 작성(JSON), Role 기반 접근
8. CloudWatch (모니터링)
- 로그/지표 수집, 알람 설정
9. Auto Scaling & Load Balancer 기초
- ALB/NLB 개념, 헬스체크, 스케일링 정책
10. Route 53 (DNS)
- 도메인 연결, 레코드 타입, 헬스체크 기반 라우팅
11. 백업/복구 & 장애 대응
- 스냅샷 정책, 리전 간 복구 시나리오, RTO/RPO 개념
12. CLI & 자동화 기초
- AWS CLI, boto3 기초 연동
13. 비용 관리 기초
- Cost Explorer, 예산 알람
14. 서버리스 개념 맛보기 (선택 항목)
- Lambda 기본 개념, 이벤트 기반 실행 구조

실무 적용 훈련 방식
매일 1개의 [현업 실무 시나리오 문제] 출제
이론 설명 → 실전 문제 → AWS 콘솔/CLI 조합 도출까지 실습형으로 진행

---

Track 8

[가상화/컨테이너/오케스트레이션 파트] 스코프 정리 (개정 — 인프라 엔지니어→데브옵스 확장 반영)
목표 (Goal)
사내 서버 가상화 환경을 이해하는 기초 체력 + 개발/자동화에 바로 활용 가능한 Docker 실전 역량 + 컨테이너 오케스트레이션(Kubernetes) 실습 가능 수준까지 기른다.
→ 기존에는 "K8s 개념만 가볍게"였으나, 인프라 엔지니어→데브옵스 커리어 확장 목표에 맞춰 실습 가능한 수준까지 심화한다.

우선순위
전체 14개 트랙 중 2순위 (진행 순서 8번, 주말 진행)
- VMware/Hyper-V는 사내 실사용 여부 확인 후 우선순위 재조정 (안 쓰면 가볍게, Docker/K8s에 비중 집중)

학습 순서 및 핵심 키워드
0. [Top-Down 오리엔테이션]
- "코드 → 이미지 빌드 → 레지스트리 push → 클러스터가 이미지 pull해서 Pod로 실행 → 서비스가 트래픽을 받음 → 모니터링이 지켜봄"까지 컨테이너 기반 배포/운영 전체 흐름을 먼저 그림 한 장으로 본다
1. 가상화 기본 개념
- 하이퍼바이저 개념, 가상머신 vs 컨테이너 차이
2. VMware / Hyper-V 기초 (사내 인프라 확인 후 진행)
- VM 생성/스냅샷/클론, vSwitch 개념
3. VM 트러블슈팅 기초
- VM 응답 없음/느려짐 진단, 리소스 경합 진단
4. Docker 기본 개념
- 컨테이너 vs VM, 이미지/컨테이너 개념, Dockerfile 기초
5. Docker 실전 명령어 & 데이터 영속성
- docker build/run/ps/exec/logs, 볼륨 백업 전략, 데이터 유실 방지
6. Docker Compose
- 여러 컨테이너(웹앱+DB) 구성, docker-compose.yml 작성
7. 컨테이너 네트워킹 기초
- 컨테이너 간 통신, 포트 매핑
8. 이미지 관리 & 배포 기초
- Docker Hub, 이미지 태깅/버전 관리
9. 컨테이너 기반 실전 활용 (웹개발 트랙 연계)
- 필수 3종 중 하나를 Docker Compose로 컨테이너화
10. Kubernetes 기본 개념 (심화 — 기존 "선택 항목"에서 승격)
- Pod, Deployment, Service, ReplicaSet 개념, 클러스터/노드/컨트롤 플레인 구조
11. Kubernetes 로컬 실습 환경 구축
- minikube 또는 kind로 로컬 클러스터 구성
12. Kubernetes 실전 배포 & 운영
- kubectl 명령어(apply/get/describe/logs/exec), Deployment/Service 매니페스트(YAML) 작성 및 배포
13. Kubernetes 설정 관리
- ConfigMap, Secret으로 설정값/민감정보 분리 관리
14. Kubernetes 기본 트러블슈팅
- Pod가 안 뜰 때 진단 순서(kubectl describe/logs), 이미지 pull 실패, 리소스 부족 진단
15. 오케스트레이션 심화 맛보기 (선택 항목)
- Helm 기초, Ingress 개념

실무 적용 훈련 방식
매일 1개의 [현업 실무 시나리오 문제] 출제
이론 설명 → 실전 문제 → 명령어/설정 조합 도출까지 실습형으로 진행

---

Track 9

[모니터링 & 백업/복구 파트] 스코프 정리
목표 (Goal)
장애를 사전에 감지하고, 장애 발생 시 신속하고 정확하게 복구할 수 있는 운영자 감각을 기른다.

목표 수준 안내: 도구/명령어 항목은 완전 숙달, 설계·판단이 필요한 항목(알림 우선순위, 백업 대상 우선순위, RTO/RPO, 장애 등급 분류)은 "판단 기준의 틀을 이해"하는 정도가 목표.

우선순위
전체 14개 트랙 중 1순위급 (진행 순서 9번, 평일 짬짬이 진행)

학습 순서 및 핵심 키워드
0. [Top-Down 오리엔테이션]
- "임계치 감지 → 알림 발생 → 대응 → 필요시 백업에서 복구"까지 운영 전체 흐름을 큰 그림으로 먼저 본다
1. 모니터링 기본 개념
- 능동 vs 수동 모니터링, 임계치 설정 원칙, 오탐 관리
2. 시스템 리소스 모니터링 (OS 레벨 통합)
- CPU/메모리/디스크/네트워크를 지속 관찰하는 관점
3. Zabbix / Nagios류 모니터링 툴 기초 + 시각화
- 에이전트 설치/등록, 트리거 설정, Grafana 등 대시보드 시각화
4. 알림 체계 구축 (개념 이해 위주)
- 알림 우선순위 구분 기준, 알림 폭탄(alert fatigue) 방지 원칙 이해
5. 로그 기반 모니터링
- 애플리케이션/시스템 로그 실시간 감시
6. 서비스 가용성 모니터링
- uptime 체크, 포트 오픈 여부, 헬스체크 개념
7. 백업 정책 수립 기초 (개념 이해 위주)
- 전체/증분/차등 백업, 3-2-1 백업 원칙, 백업 대상 우선순위 기준 이해
8. 실전 백업 명령/도구
- rsync, tar, mysqldump / Windows Backup, robocopy / RDS 스냅샷
9. 백업 검증 & 복구 테스트
- "복구가 실제로 되는지" 정기 검증
10. RTO/RPO 개념 및 설계 (개념 이해 위주)
- 복구 목표 시간/시점 정의, 서비스 중요도별 차등 설계 기준 이해
11. 장애 복구 절차 (DR) 기초 (개념 이해 위주)
- 장애 등급 분류 기준, 복구 우선순위 결정 방식
12. 클라우드 백업/복구 (AWS 연계)
- EBS 스냅샷 자동화, S3 라이프사이클 정책

실무 적용 훈련 방식
매일 1개의 [현업 실무 시나리오 문제] 출제
도구/명령어 항목: 이론 → 실전 문제 → 설정/명령어 조합 도출
개념 이해 항목: 판단 기준의 틀만 이해, 실제 판단력은 현업 경험으로 축적

---

Track 10

[Git / 버전관리 파트] 스코프 정리
목표 (Goal)
코드 변경 이력을 안전하게 관리하고 필요시 되돌릴 수 있는 실전 역량을 기른다.

우선순위
전체 14개 트랙 중 최우선, 독립 트랙 (진행 순서 10번, 주말 진행)

학습 순서 및 핵심 키워드

0. [Top-Down 오리엔테이션]
- working directory → staging area → local repository → remote repository로 이어지는 코드 흐름을 그림으로 먼저 본다

선행 학습 (3~5일 내 완료) — **1~5번 완료** (2026-07-20 완료)
1. Git 기본 개념
- 버전관리란 무엇인가, 로컬 vs 원격 저장소, working directory/staging area/repository 3단계 구조
2. 기본 명령어 (로컬)
- git init, add, commit, status, log, diff
3. 브랜치(Branch) 기초
- git branch, checkout/switch, merge 기초
4. .gitignore 활용
- 민감정보/빌드 산출물 제외 관리, 원격 push 전 반드시 숙지
5. 원격 저장소 연동
- git remote, push, pull, clone, GitHub/GitLab 활용

심화 (실전 중 필요할 때 학습)
6. 충돌(Conflict) 해결
- merge conflict 발생 상황 재현 및 해결 절차
7. Git Stash 활용
- 작업 중 변경사항 임시 저장, 급하게 다른 브랜치 확인해야 할 때 대응
8. 커밋 메시지 컨벤션
- 좋은 커밋 메시지 작성 원칙, Conventional Commits
9. 되돌리기 & 이력 관리 실전
- git reset vs revert, 특정 커밋으로 되돌리기
10. 실전 협업 워크플로우 기초 (최저 우선순위)
- Pull Request/Merge Request 개념, Git Flow 맛보기
11. 실전 적용 (웹개발/자동화 트랙 연계, 지속)
- 필수 3종 프로젝트, 자동화 스크립트를 Git으로 관리

실무 적용 훈련 방식
웹개발/자동화 코드를 작성하는 순간부터 Git으로 관리하며 자연스럽게 체득
선행 학습(1~5번)은 짧게 별도 실습으로 익히고 시작
충돌 해결(6번), 되돌리기(9번)는 의도적으로 문제 상황을 만들어 실전처럼 연습

---

Track 11 (주말 병렬 트랙)

[HTML/CSS/JS 파트] 스코프 정리
목표 (Goal)
사내 웹툴(게시판, 예약 시스템, 티켓/위키 등)의 화면을 직접 만들고 백엔드 API와 연동할 수 있는 실전 프론트엔드 최소 역량을 기른다.

우선순위
주말 병렬 트랙 (Java/Spring/SpringBoot과 묶어서 진행, 진행은 병행하되 기록은 독립 저장)

학습 순서 및 핵심 키워드
0. [Top-Down 오리엔테이션]
- 브라우저가 HTML/CSS/JS를 어떻게 렌더링하고, 사용자 액션이 어떻게 서버 API 호출로 이어지는지 큰 그림으로 먼저 본다
1. HTML 기본 구조 & 시맨틱 태그
- semantic 태그, form 요소, 실무 구조 패턴
2. CSS 기본 & 레이아웃
- Box model, Flexbox, Grid
3. CSS 반응형 & 실전 스타일링
- media query, CSS 변수 활용
4. JavaScript 기본 문법
- 변수, 조건문/반복문, 함수, 배열/객체 메서드
5. DOM 조작
- querySelector, addEventListener, 동적 요소 생성/삭제
6. 비동기 처리 & API 연동 + 디버깅
- fetch API, async/await, JSON 파싱, 브라우저 개발자도구(콘솔/네트워크탭) 디버깅
7. 상태 관리 기초 (Vanilla JS 수준)
- 화면 상태를 변수/객체로 관리, SPA 흉내내기 수준
8. 편의 라이브러리 활용
- Bootstrap/Tailwind, FullCalendar(예약), Marked.js(위키)
9. 폼 처리 & 유효성 검증
- 클라이언트 사이드 검증
10. 프레임워크 맛보기 (선택 항목)
- React 또는 Vue 기초

실무 적용 훈련 방식
Java/Spring 트랙의 필수 3종 구현 과정에서 필요한 화면을 그때그때 실전으로 학습, 백엔드와 동시 진행

---

Track 12 (주말 병렬 트랙)

[Java / Spring / SpringBoot 파트] 스코프 정리
목표 (Goal)
Spring 전문 개발자가 아니라, CRUD 기반 사내 웹앱(기존 레거시 유지보수 포함)을 혼자 기획-개발-배포까지 할 수 있는 수준을 목표로 한다.

우선순위
주말 병렬 트랙 (HTML/CSS/JS 트랙과 묶어서 진행)

학습 순서

0. [Top-Down 오리엔테이션]
- 요청이 Controller → Service → Repository → DB로 이어지고 다시 응답으로 돌아오는 Spring Boot 전체 요청 흐름을 큰 그림으로 먼저 본다

0단계 — Java 언어 기초 (선행)
1. 변수/자료형 & 연산자
2. 제어문 (조건문/반복문)
3. 배열 & 문자열
4. 클래스와 객체 기초 (생성자, this, 접근제어자)
5. 객체지향 심화 (상속, 오버라이딩/오버로딩, static, 다형성)
6. 인터페이스 & 추상클래스
7. 컬렉션 프레임워크 (List, Map, Set)
8. 예외 처리 (try-catch-finally, checked/unchecked)
9. 람다 & 스트림 기초

1단계 — Spring Boot 기초
10. 프로젝트 구조 이해 & Git 연동 시작 (git init, .gitignore 즉시 적용)
11. DI(의존성 주입) & IoC 개념
12. 설정 파일 & 프로퍼티 관리 (application.properties/yml)
13. JPA 엔티티 매핑 기초 (@Entity, @Id, @GeneratedValue)
14. JPA 연관관계 매핑 (@OneToMany, @ManyToOne, 지연/즉시로딩)
15. Repository & 쿼리 작성 (쿼리 메서드, @Query)
16. REST API 설계 기초 & API 테스트 (Postman 활용)
17. 트랜잭션 기초 (@Transactional)
18. 예외 처리 & 공통 응답 구조 (@ExceptionHandler, @ControllerAdvice)
19. 로그인/인증 기초 (세션 또는 Spring Security 최소 적용, BCrypt, role 구분)
20. DTO & Entity 분리

2단계 — 필수 구현 목표 3종 (난이도순)
1. 게시판 CRUD (+ 로그인 포함)
- 생성/조회/수정/삭제, 페이징, 로그인 사용자만 글쓰기
2. 회의실 예약 시스템
- CRUD + 시간 충돌 검증 로직 + 캘린더 UI
3. 티켓 관리 시스템 + 간단 위키
- CRUD + 상태 전이(Enum) + 담당자/우선순위, 버전 관리 + 마크다운 렌더링, 예외 처리 심화

3단계 — 간단 배포
- 로컬을 넘어 실제 접속 가능한 환경에 올려보기 (jar 빌드, 실행, 기본 외부 접속 설정)
- (Track 8/14 연계) 필수 3종 중 하나는 Docker Compose 컨테이너화 → CI/CD 파이프라인으로 자동 배포까지 확장 (포트폴리오 핵심 산출물)

보류 항목 (필수 3종 완성 후 검토)
- Thymeleaf 기초 (선택)
- 실시간 채팅 (WebSocket)
- 메일 발송 연동 (SMTP)

실무 적용 훈련 방식
0~1단계는 짧고 빠르게 순서대로 훑되 짧은 코드로 직접 쳐보며 익힘
2단계는 HTML/CSS/JS 트랙과 동시 진행하며 실전 적용

---

Track 13 (주말 병렬 트랙)

[SQL 파트] 스코프 정리
목표 (Goal)
DB 이론 암기가 아니라, 웹개발 트랙(게시판/예약/티켓+위키)의 백엔드 DB 설계 및 쿼리를 직접 짤 수 있는 실전 역량을 기른다.

우선순위
주말 병렬 트랙 (Java/Spring 트랙과 밀접 연계, 프로젝트 진행과 병행)

학습 순서 및 핵심 키워드

0. [Top-Down 오리엔테이션]
- 애플리케이션 요청이 쿼리로 변환되어 테이블에서 데이터를 읽고 쓰는 전체 흐름(테이블 설계 → 쿼리 → 트랜잭션 → 인덱스)을 큰 그림으로 먼저 본다

1단계 — 기본 조회 문법
1. 기본 SELECT 문법 (SELECT/FROM/WHERE, ORDER BY, LIMIT/OFFSET)
2. 조건절 심화 (AND/OR/IN/BETWEEN/LIKE, NULL 처리)
3. 집계 함수 & GROUP BY (COUNT/SUM/AVG/MAX/MIN, HAVING)
4. JOIN 종류 및 활용 (INNER/LEFT/RIGHT JOIN)
5. 서브쿼리 & 실전 쿼리 패턴 (EXISTS, 최신 N개 조회 등)

2단계 — 테이블 설계 (DDL)
6. 테이블 설계 기초 & ERD
- CREATE TABLE, PK/FK, JPA 엔티티 ↔ SQL 테이블 매핑 관계, ERD 작성 습관화
7. 제약조건(Constraints)
- NOT NULL, UNIQUE, DEFAULT, CHECK
8. 외래키 삭제/수정 정책
- ON DELETE CASCADE/SET NULL/RESTRICT
9. 문자 인코딩 설정
- utf8mb4, 한글/이모지 데이터 처리

3단계 — 데이터 조작 & 성능
10. 데이터 조작 (DML) & 트랜잭션
- INSERT/UPDATE/DELETE, COMMIT/ROLLBACK 직접 실습
11. 인덱스 기초
- 인덱스 개념과 조회 성능, EXPLAIN 활용

4단계 — 프로젝트 연계 심화
12. 버전 관리 로직 구현 (위키 트랙 연계)
- 이력 테이블 설계 패턴 (history 테이블 + 버전 컬럼 + FK)
13. 상태 전이 데이터 설계 (티켓 트랙 연계)
- 상태값(Enum/코드 테이블) 설계
14. 시간/날짜 데이터 처리
- DATE/DATETIME, 날짜 비교/범위 조회
15. DB 스키마 변경 이력 관리 (개념 이해 위주)
- Flyway/Liquibase 개념 소개

5단계 — 백업 (비중 축소)
16. 백업/복구 기초 (가볍게, DB 자체 명령 관점)
- mysqldump 등 기본 백업 명령 실습

실무 적용 훈련 방식
Java/Spring 트랙의 필수 3종 구현 과정에서 필요한 테이블/쿼리를 그때그때 실전으로 학습
1단계 기본 문법은 실무 시나리오 형태로 별도 연습 가능

---

Track 14 (신규 — 주말 병렬 트랙)

[CI/CD 파트] 스코프 정리
목표 (Goal)
인프라 엔지니어에서 데브옵스 엔지니어로 커리어를 확장하기 위한 핵심 역량. 코드 변경이 자동으로 빌드-테스트-배포까지 이어지는 파이프라인을 설계하고 운영할 수 있는 실전 감각을 기른다. Track 8(Docker/K8s), Track 10(Git), Track 12(Spring 배포)와 직접 연계된다.

우선순위
전체 14개 트랙 중 2순위 (진행 순서 8번 Track 8 직후, 주말 진행)

학습 순서 및 핵심 키워드
0. [Top-Down 오리엔테이션]
- "코드 push → 파이프라인 트리거 → 빌드 → 테스트 → 이미지 생성 → K8s 배포"까지 전체 CI/CD 흐름을 그림 한 장으로 먼저 본다
1. CI/CD 개념 및 배경
- 지속적 통합(CI)/지속적 배포(CD) 개념, 수동 배포 대비 이점, 왜 필요한가
2. Jenkins 기초
- 설치, Job 생성, Freestyle Job vs Pipeline Job 차이
3. Jenkinsfile 작성 (Declarative Pipeline)
- stage/step 문법, 파이프라인 코드화(Pipeline as Code)
4. GitLab CI 기초
- .gitlab-ci.yml 문법, Runner 개념, stages/jobs 구조
5. 빌드 자동화 (Track 8 연계)
- 파이프라인에서 Docker 이미지 빌드 및 레지스트리(Docker Hub 등) push 자동화
6. 테스트 자동화 연계
- 파이프라인에 단위 테스트 실행 단계 포함, 실패 시 파이프라인 중단
7. 배포 자동화 (Track 8 연계)
- kubectl apply/rollout으로 K8s 클러스터에 자동 배포
8. 배포 전략 개념 (개념 이해 위주)
- Blue-Green, Rolling Update, Canary 배포 — 각 전략의 장단점과 선택 기준
9. Secret/Credential 관리 기초
- Jenkins Credentials, GitLab CI Variables로 민감정보(DB 비밀번호, API 키 등) 분리 관리
10. 파이프라인 실패 시 트러블슈팅
- 빌드/배포 로그 분석, 실패 지점 추적, 재시도 처리
11. 실전 적용 (Track 12 연계)
- 필수 3종 웹앱 중 하나를 Git push 한 번으로 빌드-테스트-Docker 이미지화-K8s 배포까지 자동으로 이어지는 파이프라인 완성 (포트폴리오 핵심 산출물)

실무 적용 훈련 방식
매일/매주 [현업 실무 시나리오 문제] 출제
이론 설명 → 실전 문제 → Jenkinsfile/.gitlab-ci.yml 작성까지 실습형으로 진행
8번(배포 전략)은 개념 이해 위주로 진행하며, 명령어 도출을 요구하지 않고 "왜 이 전략을 쓰는지" 판단 기준만 짚는다

---

## 진행 방식 요약

평일(회사, 짬짬이): 컴퓨터구조 → 네트워크 → 리눅스 → 윈도우 → 보안 → 모니터링&백업
주말: Git(선행) → HTML/CSS/JS + Java/Spring/SpringBoot + SQL (병행) → 자동화 → AWS → 가상화/컨테이너/오케스트레이션(Docker+K8s) → CI/CD

전체 목표 마감: 2027년 6월 21일
커리어 목표: 인프라 엔지니어(정규직 취업) → 데브옵스 엔지니어로 커리어 확장
자격증 병행 순서: CCNA → RHCSA → AWS SAA (확정) / CKA, Docker DCA (후보군, 여유 시 검토)